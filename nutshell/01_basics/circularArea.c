// circularArea.c: Calculate and print the areas of circles

// Calculate the area of a circle
// Param: r: the circle's radius
// Return: the circle's area
double circularArea(double r) {
  const double PI = 3.1415926536;
  return PI * r * r;
}
